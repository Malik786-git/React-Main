import React from "react";
// const MyContext = React.createContext('kuch bh.');
// export const Provider = MyContext.Provider;
// export const Consumer = MyContext.Consumer;


// for context type no need consumer, but need to export MyContext.
export const MyContext = React.createContext('kuch bh.');
export const Provider = MyContext.Provider;
